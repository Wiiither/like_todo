import 'package:flutter/material.dart';

class TodoTagListContentItemView extends StatelessWidget {
  const TodoTagListContentItemView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 200,
      color: Colors.blue,
    );
  }
}
